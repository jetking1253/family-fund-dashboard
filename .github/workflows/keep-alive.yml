name: Keep Alive - Prevent Supabase Pause
on:
  schedule:
      - cron: '0 2 1,15 * *'
        workflow_dispatch:
        jobs:
          keep-alive:
              runs-on: ubuntu-latest
                  steps:
                        - run: curl -L "${{ secrets.SUPABASE_URL }}"
                        
